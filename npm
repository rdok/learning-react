 #!/usr/bin/bash

ttyArg=$([ -t 1 ] && echo "--ty" || echo "")

docker run \
    --user $(id -u) \
    --workdir /app \
    --volume $(pwd):/app \
    --rm  \
    --interactive \
    $ttyArg \
    node \
    npm "$@"
